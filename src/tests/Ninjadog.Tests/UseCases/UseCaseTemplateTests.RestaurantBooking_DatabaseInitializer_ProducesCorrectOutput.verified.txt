//------------------------------------------------------------------------------
// This code was powered by the Ninjadog Engine
//
// Developed by: Atypical Consulting SRL - Philippe Matray
// Generated on: [SCRUBBED]
// Version     : [SCRUBBED]
//
// This file is part of a custom software solution crafted to meet your
// specific needs. For optimal performance and compatibility, modifications
// should be coordinated with Ninjadog support services.
//------------------------------------------------------------------------------

using Dapper;

namespace RestaurantBooking.CrudWebApi.Database;

public partial class DatabaseInitializer(IDbConnectionFactory connectionFactory)
{
    public async Task InitializeAsync()
    {
        using var connection = await connectionFactory.CreateConnectionAsync();
        
        await connection.ExecuteAsync(@"CREATE TABLE IF NOT EXISTS Customers (
            CustomerId CHAR(36) PRIMARY KEY,
            CustomerFirstName TEXT,
            CustomerSurname TEXT,
            PhoneNumber TEXT,
            CellphoneNumber TEXT,
            EmailAddress TEXT,
            OtherCustomerDetails TEXT)");

        await connection.ExecuteAsync(@"CREATE TABLE IF NOT EXISTS Bookings (
            BookingId CHAR(36) PRIMARY KEY,
            DateOfBooking TEXT,
            NumberInParty INTEGER)");

        await connection.ExecuteAsync(@"CREATE TABLE IF NOT EXISTS Tables (
            TableNumber INTEGER PRIMARY KEY,
            TableDetails TEXT)");

        await connection.ExecuteAsync(@"CREATE TABLE IF NOT EXISTS Orders (
            OrderId CHAR(36) PRIMARY KEY,
            OrderDateTime TEXT)");

        await connection.ExecuteAsync(@"CREATE TABLE IF NOT EXISTS OrderMenuItems (
            OrderMenuItemId CHAR(36) PRIMARY KEY,
            OrderMenuItemQuantity INTEGER,
            OrderMenuItemComments TEXT)");

        await connection.ExecuteAsync(@"CREATE TABLE IF NOT EXISTS Menus (
            MenuId CHAR(36) PRIMARY KEY,
            MenuDate TEXT)");

        await connection.ExecuteAsync(@"CREATE TABLE IF NOT EXISTS MenuItems (
            MenuItemId CHAR(36) PRIMARY KEY,
            MenuItemDescription TEXT,
            MenuItemPrice REAL)");

        await connection.ExecuteAsync(@"CREATE TABLE IF NOT EXISTS MenuItemIngredients (
            MenuItemIngredientId CHAR(36) PRIMARY KEY,
            ItemQuantity INTEGER)");

        await connection.ExecuteAsync(@"CREATE TABLE IF NOT EXISTS Ingredients (
            IngredientId CHAR(36) PRIMARY KEY,
            IngredientName TEXT)");

        await connection.ExecuteAsync(@"CREATE TABLE IF NOT EXISTS IngredientTypes (
            IngredientTypeCode CHAR(36) PRIMARY KEY,
            IngredientTypeDescription TEXT)");

        await connection.ExecuteAsync(@"CREATE TABLE IF NOT EXISTS Staffs (
            StaffId CHAR(36) PRIMARY KEY,
            StaffFirstName TEXT,
            StaffLastName TEXT)");

        await connection.ExecuteAsync(@"CREATE TABLE IF NOT EXISTS StaffRoles (
            StaffRoleCode CHAR(36) PRIMARY KEY,
            StaffRoleDescription TEXT)");

    }
}
