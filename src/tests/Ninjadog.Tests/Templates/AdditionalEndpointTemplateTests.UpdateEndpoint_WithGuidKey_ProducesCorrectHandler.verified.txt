//------------------------------------------------------------------------------
// This code was powered by the Ninjadog Engine
//
// Developed by: Atypical Consulting SRL - Philippe Matray
// Generated on: [SCRUBBED]
// Version     : 2.0.0.0
//
// This file is part of a custom software solution crafted to meet your
// specific needs. For optimal performance and compatibility, modifications
// should be coordinated with Ninjadog support services.
//------------------------------------------------------------------------------

using TestApp.Api.Contracts.Requests;
using TestApp.Api.Contracts.Responses;
using TestApp.Api.Mapping;
using TestApp.Api.Services;
using FastEndpoints;
using Microsoft.AspNetCore.Authorization;

namespace TestApp.Api.Endpoints;

public partial class UpdateTodoItemEndpoint
    : Endpoint<UpdateTodoItemRequest, TodoItemResponse>
{
    public ITodoItemService TodoItemService { get; private set; } = null!;

    public override void Configure()
    {
        Put("/todo-items/{id:guid}");
        AllowAnonymous();
    }

    public override async Task HandleAsync(UpdateTodoItemRequest req, CancellationToken ct)
    {
        var existingTodoItem = await TodoItemService.GetAsync(req.Id);

        if (existingTodoItem is null)
        {
            await SendNotFoundAsync(ct);
            return;
        }

        var todoItem = req.ToTodoItem();
        await TodoItemService.UpdateAsync(todoItem);

        var todoItemResponse = todoItem.ToTodoItemResponse();
        await SendOkAsync(todoItemResponse, ct);
    }
}
