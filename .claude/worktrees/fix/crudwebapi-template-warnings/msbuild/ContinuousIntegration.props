<Project>
  <!-- Continuous Integration -->
  <PropertyGroup Label="Deterministic Build" Condition="'$(CI)' == 'true'">
    <ContinuousIntegrationBuild>true</ContinuousIntegrationBuild>
  </PropertyGroup>
</Project>
